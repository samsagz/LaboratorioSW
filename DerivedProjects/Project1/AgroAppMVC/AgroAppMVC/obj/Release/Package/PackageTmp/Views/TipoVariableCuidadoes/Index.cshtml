@model IEnumerable<AgroAppMVC.Models.TipoVariableCuidado>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreTipoVariableCuidado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreTipoVariableCuidado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoVariableCuidadoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoVariableCuidadoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoVariableCuidadoId })
        </td>
    </tr>
}

</table>
